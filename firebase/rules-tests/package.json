{
  "name": "nirog-bhumi-rules-tests",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Firestore security rules unit tests, run against the local emulator (never production).",
  "scripts": {
    "test": "node --test firestore.rules.test.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
