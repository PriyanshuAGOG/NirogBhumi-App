<?xml version="1.0" encoding="utf-8"?>
<!-- OS-mandated placeholder layout, shown for the brief moment before
     Glance's own composed content (HealthQuickLogWidget) takes over -
     every AppWidgetProviderInfo needs a real initialLayout resource even
     when the actual UI is entirely Glance-composed. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" />
